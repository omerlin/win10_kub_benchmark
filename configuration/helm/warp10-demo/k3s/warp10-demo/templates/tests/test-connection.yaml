apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "warp10-demo.fullname" . }}-test-connection"
  labels:
    {{- include "warp10-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "warp10-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
